DATABASE_URL="postgresql://username:password@localhost:5432/homework_db"
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_EXPIRES_IN="7d"
UPLOAD_MAX_SIZE="10485760"
NODE_ENV="development"
PORT="5050"
# CORS: frontend adresi (canlıda https://your-app.vercel.app veya virgülle ayrılmış liste)
FRONTEND_URL="http://localhost:5173"
# Rate limit: 15 dakikada max istek (varsayılan 600). 429 alıyorsanız Render'da 800–1000 yapın.
# RATE_LIMIT_MAX="600"
# Vercel Blob (dosya/ses yükleme). Yoksa /api/upload/blob 503 döner.
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"
